function getRandomInt(max) {
    return Math.floor(Math.random() * max);
  }

console.log('on joue au "pierre papier ciseaux" ')
console.log('choisi celui que tu veux et je vais en choisir un aussi')
let tab = ["pierre", "papier", "ciseaux"]

let bleubleu = getRandomInt(3)















